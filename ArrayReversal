

function ArrayRevesral(arr) {
	const n = arr.length;
	const result = new Array(n);
	for (let i = 0; i < n; i++) {
		result[i] = arr[n - 1 - i];
	}
	return result;
}
 // Example
 cosole.log(ArrayReversal([1, 2, 3, 4, 5]));  //[5, 4, 3, 2, 1]